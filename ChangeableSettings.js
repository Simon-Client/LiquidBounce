//Gomme-Reverse-Velocity v1.0

//SpeedCheck

var CheckForSpeed = true;

//BackPushValue
/ Reverse the velue (1.1, 1.05, 1.0) for the push velocity /
// 1.2 = maximum, any changes more than 1.2 does not work. //
// Limit = false for any changes more than 1.2 //

var BackPushValue = 1.2; // Standart Gomme Value
var Limit = true;        // Limit = 1.2


//Stabilization & AutoReversing & Checks
/ AutoReversing - player moving automatically when get damaged, aka "super" Xanax velo /
var RightStabilization = true;
var LeftStabilization = true;
var AutoReversing = true;
var AllowSneak = false;
var AllowJump = false;
